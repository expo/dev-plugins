{
  "name": "@dev-plugins/metro-bundle-plugin",
  "version": "0.0.1",
  "description": "Inspect the bundle stats from Metro",
  "keywords": [
    "expo",
    "devtools"
  ],
  "bin": "build/src/cli/bin.js",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build",
    "dist",
    "expo-module.config.json",
    "metro.js",
    "metro.d.ts",
    "webui/dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "expo-module build",
    "build:all": "expo-module prepare && cd webui && npx expo export -p web --output-dir ../dist",
    "clean": "expo-module clean",
    "prepare": "expo-module prepare",
    "prepublishOnly": "expo-module prepare && expo-module prepublishOnly && cd webui && npx expo export -p web --output-dir ../dist",
    "web:dev": "cd webui && npx expo start -w",
    "web:export": "cd webui && npx expo export -p web --output-dir ../dist"
  },
  "license": "MIT",
  "dependencies": {
    "@expo/server": "^0.3.1",
    "compression": "^1.7.4",
    "expo-router": "~3.4.7",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "expo": "~50.0.1",
    "expo-module-scripts": "^3.1.0",
    "typescript": "^5.1.3"
  },
  "peerDependencies": {
    "expo": "*"
  }
}
